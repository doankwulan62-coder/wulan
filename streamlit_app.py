import streamlit as st

st.title("🎈 aplikasi ankim kalcer")
import streamlit as st

number = st.number_input(
    "Insert a number", value=None, placeholder="Type a number..."
)
st.write("The current number is ", number)
import streamlit as st

st.button("penambahan","pengurangan")
if tambah st.button(+):
    st.tambah(tambah)
else:
    st.write("Goodbye")

if kurang st.button(-):
    st.tambah(+)
import streamlit as st

number = st.number_input(
    "Insert a number", value=None, placeholder="Type a number..."
)
st.write("The current number is ", number)
